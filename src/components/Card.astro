---
import '../css/base.css';
const offre = Astro.props;
---

<div>
    <h2>{offre.nomMaison}</h2>
    <img src={offre.img} alt="/" class: "block rounded-t-lg overflow-hidden"/>
    <a href={`/offres/${offre.id}`}>Voir plus</a>
</div>