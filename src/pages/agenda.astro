---
import Base from "../layouts/Base.astro";
import Card from "../components/Card.astro";
import { events } from "../backend.mjs"

console.log(events);
---
<Base>
    {events.map((event) => (
        <Card title={event.title} date={event.date} favori={event.favori} />
    )
)}
</Base>